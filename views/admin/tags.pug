extends ../layout

block content
	form(method="post")
		input(type="text" placeholder="create a new tag" name="name")
		input(type="submit")
	hr
	if tags.length > 0
		each tag in tags
			p #{tag.name}